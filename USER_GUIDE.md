# 📘 Nikki Conlang Forge 使用指南

## 这个应用能做什么？

Nikki Conlang Forge 是一款**自创语言翻译器**——你可以用它把你自己设计的虚构语言（自创语）和中文互相翻译。它支持：

- 🔤 **规则翻译**：基于词库、映射规则、翻译历史自动翻译
- 🤖 **AI 辅助翻译**：借助 PaperHub AI 补全词库未覆盖的内容、生成新词建议
- 🗣️ **发音输出**：自动生成 TTS 音译和国际音标（IPA）读音
- 📊 **批量翻译**：导入 Excel 台本，批量翻译整列台词并导出结果
- 💬 **语言大师问答**：直接向 AI 提问关于当前语言的问题
- 📦 **语言包管理**：新增、重命名、导出/导入语言包

---

## 快速开始

### 第一步：安装与启动

1. 确保已安装 Python 3.8+ 和 pip
2. 打开终端，进入项目目录，执行：
   ```
   pip install -r requirements.txt
   ```
3. 运行应用：
   ```
   python main.py
   ```
4. 或直接双击 `启动.bat`

### 第二步：配置 AI（可选，推荐）

如果你想使用 AI 辅助翻译和语言大师问答功能，需要先配置 PaperHub：

5. 点击菜单栏 **设置 → PaperHub 设置**
6. 填写你的 **API Key**（从 [PaperHub 工作台](https://tc-paperhub.diezhi.net/dashboard) 获取，选择 `llm_api` 类型）
7. 选择 **模型**（如 deepseek-v4-pro、qwen3-max、glm-5 等）
8. 选择 **翻译策略**：
   - **仅未匹配**：先规则翻译，词库外的内容由 AI 补全（推荐入门使用）
   - **全部 AI**：整句由 AI 翻译，参考词库保持一致性
   - **候选确认**：AI 生成候选翻译，你逐条确认后才入库
9. 可开启 **流式输出**，翻译过程中实时看到生成内容
10. 点击保存

> 💡 不配置 AI 也可以使用纯规则翻译，只要词库够丰富就能正常工作。（但不建议，因为词库肯定不够）

---

## 认识界面：三大工作区

Nikki Conlang Forge 的界面分为 **左侧面板** 和 **右侧面板**，通过可拖拽的分隔线调整宽度比例：

| 区域 | 位置 | 作用 |
|------|------|------|
| **语言区** | 左侧上方 | 选择/管理你的自创语言 |
| **当前语言资料区** | 左侧下方 | 查看/导入当前语言的资料文件 |
| **翻译工作区** | 右侧「翻译」页签 | 单句翻译或批量翻译 |
| **语言大师问答** | 右侧「语言大师问答」页签 | 与 AI 对话讨论语言创作 |

右侧面板顶部有两个页签按钮——**翻译** 和 **语言大师问答**，点击即可切换。翻译页签内又分为「单句翻译」和「批量翻译」两个子区域。

---

## 核心操作

### 一、语言管理（左侧面板 · 语言区）

左侧面板上方是你的语言列表，每条语言显示名称、备注和图标（如果有）。

| 操作 | 方法 |
|------|------|
| **新增语言** | 点击「+ 新增」按钮，或菜单 **编辑→新增语言** |
| **切换语言** | 单击列表中的语言名，右侧翻译区和资料区随之刷新 |
| **重命名** | 双击语言名，或右键→重命名，或菜单 **编辑→重命名当前语言** |
| **添加备注** | 右键→备注，或菜单 **编辑→语言备注**（可记录设计理念、灵感来源等） |
| **选择图标** | 右键→选择图标，从 `assets/lang_icons/` 目录选择一张图片作为语言头像 |
| **删除语言** | 右键→删除（至少保留一种语言） |
| **导出语言包** | 右键→导出语言包→保存为 ZIP |
| **导入语言包** | 右键→导入语言包→选择 ZIP 文件 |

> 💡 导出语言包会将当前语言的全部资料打包为 ZIP，方便分享给其他用户。对方导入后即可直接使用该语言的所有资料。

---

### 二、导入语言资料（左侧面板 · 资料区）

左侧面板下方是当前语言的资料状态区，展示四种资料文件的加载状态：

| 资料 | 格式 | 说明 |
|------|------|------|
| **白皮书** | `.md` / `.txt` | 语言的世界观、语法规则、文化背景描述——AI 翻译的"知识来源" |
| **主词库** | `.json` | 中文↔自创语的词汇对照表——规则翻译的"查词表" |
| **映射表** | `.csv` | 自创语→IPA→TTS 音译的映射规则——语音合成和音标生成依据 |
| **翻译历史** | `.json` | 已确认的翻译记录——规则翻译时优先查找历史匹配 |

每个资料前有状态标记：
- **●**（绿色实心圆）= 已导入且有效
- **○**（空心圆）= 尚未导入或文件不存在
- **◉**（橙色圆）= 文件存在但有错误

**导入步骤**：

11. 选择左侧某个语言
12. 点击「**导入资料**」按钮（或菜单 **文件→导入资料**）
13. 在文件选择窗口中**可多选**文件（按 Ctrl 多选）
14. 应用会根据文件名**自动识别**文件类型：
    - 含 `_Conlang_Master_Library.json` → 主词库
    - 含 `_Mapping_Rules.csv` → 映射表
    - 含 `_Translation_History.json` → 翻译历史
    - 文件名含「白皮书」或 `whitepaper` → 白皮书
15. 导入后左侧面板会显示各资料的加载状态（● / ○ / ◉）

**资料对翻译的影响**：

| 翻译类型 | 需要哪些资料 |
|----------|-------------|
| 规则翻译 | 主词库（必须有）+ 翻译历史（可选提升） |
| AI 辅助翻译 | 白皮书 + 主词库（AI 参考语言规则） |
| TTS 音译输出 | 映射表（必须有） |
| IPA 音标输出 | 映射表（必须有） |

> ⚡ 白皮书是 AI 翻译质量的关键。越详细的白皮书（音系规则、构词法、文化背景），AI 翻译越准确。

---

### 三、单句翻译（翻译页签 · 单句翻译）

这是最常用的功能——手动输入中文文本，即时获得自创语翻译。适合**逐句校验、小段文本翻译、调试词库**等场景。

**界面布局**：

| 区域 | 说明 |
|------|------|
| **中文输入**（左上） | 可多行输入中文原文 |
| **自创语输出**（右上） | 翻译后的自创语文本，`【词】` 表示词库中未匹配的词 |
| **实时生成过程** | AI 翻译时实时显示生成过程（流式模式） |
| **TTS 音译**（左下） | 供语音合成使用的友好拼写文本 |
| **国际音标读音 IPA**（右下） | IPA 国际音标标注 |
| **统计行** | 显示匹配率、未匹配词数量；若有未匹配词会显示「将未匹配词添加到词库…」按钮 |

每个输出框旁边有「**复制**」按钮，可一键复制内容。

**使用步骤**：

16. 在左侧选择一个语言并导入资料
17. 切换到右侧「**翻译**」页签
18. 在「**中文输入**」框输入中文文本（可多行）
19. 点击右侧「**翻译**」按钮
20. 查看结果：
    - 自创语输出中 `【词】` 标记表示词库中未匹配的词
    - TTS 音译和 IPA 音标会根据映射表自动生成
    - 统计行显示本次翻译的匹配率
21. 如有未匹配词 → 点击统计行的「将未匹配词添加到词库…」按钮处理

**AI 翻译策略**（需先在 PaperHub 设置中启用并选择）：

| 策略 | 说明 | 适合场景 |
|------|------|----------|
| **仅未匹配**（推荐） | 词库已匹配的部分保留，只有【词】标记的片段由 AI 补全 | 词库较完善，希望节省 API 用量 |
| **全部 AI** | 整句由 AI 翻译，参考词库保持一致性 | 需要更自然的翻译风格，或调试 AI 表现 |
| **候选确认** | AI 生成候选翻译后弹出确认对话框，可采用/修改/放弃 | 对翻译质量要求高，希望人工把关 |

> 💡 当中文输入框全部清空时，所有输出区域（自创语、TTS、IPA、实时生成）会自动同步清空。

---

### 四、处理未匹配词

翻译后如果出现未匹配词：

22. 统计行会出现「**将未匹配词添加到词库…**」按钮
23. 点击后打开**未匹配词汇处理对话框**，提供四种方式：

| 方式 | 说明 |
|------|------|
| **手动填写** | 直接为每个未匹配词填写自创语翻译和 TTS 拼写 |
| **单词 AI** | 让 AI 为单个词生成翻译（点击每行的 [AI] 按钮） |
| **批量 AI** | 让 AI 同时为所有未匹配词生成翻译（点击「全部 AI 生成」） |
| **保存到词库** | 确认填写内容后，自动写入主词库和映射表，下次翻译直接匹配 |

> ⚡ 保存到词库后，下次翻译时词库将直接命中这些词，不再需要调用 AI。

---

### 五、语言大师问答（语言大师问答页签）

这是 AI 对话功能，你可以自由提问关于当前语言的问题——探讨翻译方案、词源创作、风格变体等。AI 会参考当前语言的白皮书和词库来回答。

**界面布局**：

| 区域 | 说明 |
|------|------|
| **对话历史**（上方） | 显示你和 AI 的对话记录；右上角有 Token 使用率圆环和「清空」按钮 |
| **待审核候选词**（中间） | AI 创造的新词会自动提取到这里，以表格形式展示；可收起/展开 |
| **提问输入**（下方） | 输入框 + 快捷提问模板按钮 + 「发送」按钮 |

**使用步骤**：

24. 切换到右侧「**语言大师问答**」页签
25. 在下方输入框输入问题，按「**发送**」或 **Ctrl+Enter** 提交
26. AI 流式回复会实时显示在对话历史区
27. 如果 AI 创造了新词（格式：`【新词】自创语|IPA|TTS|含义|风格标签`），会自动提取到待审核表格

**待审核候选词处理**：

| 操作 | 方法 |
|------|------|
| **单条确认** | 点击该行的 ✓ 确认按钮，该词入库并移除该行 |
| **单条丢弃** | 点击该行的 ✗ 丢弃按钮，移除该行 |
| **多选确认** | Ctrl/Shift 多选行 → 点击「**确认选中**」按钮 |
| **多选丢弃** | Ctrl/Shift 多选行 → 点击「**丢弃选中**」按钮 |
| **全部确认** | 点击「**全部确认**」按钮，将所有候选词导入词库 |
| **全部丢弃** | 点击「**全部丢弃**」按钮，放弃所有候选词 |

**快捷提问模板**：

输入框上方有一行快捷按钮，预设常用问题（如"帮我创造一个表达XX的词"）。点击按钮自动填入提问文本。

- 点击「**管理模板…**」可增删改模板
- 模板保存在配置文件中，下次启动自动加载

**Token 使用率圆环**：

对话区右上角的小圆环显示当前会话的 Token 消耗比例：
- **绿色**（<50%）：用量健康
- **紫色**（50-80%）：接近上限
- **红色**（>80%）：建议精简对话或清空历史

**语言大师问答适合什么需求？**

| 我想做… | 去哪里 |
|---------|--------|
| 探讨翻译方案，但不想直接翻译 | 语言大师问答 |
| 让 AI 帮我创造新词汇并解释构词逻辑 | 语言大师问答 |
| 对已有词库不满意，想讨论风格变体 | 语言大师问答 |
| 需要逐句精确翻译 | 翻译工作区 → 单句翻译 |
| 需要大批量翻译 | 翻译工作区 → 批量翻译 |

---

### 六、批量翻译（翻译页签 · 批量翻译）

适用于翻译大量台词（如游戏台本、小说段落）。批量翻译会一次性处理整个 Excel 文件中的文本列。

**界面布局**：

| 区域 | 说明 |
|------|------|
| **操作按钮行** | 选择 Excel / 开始翻译 / 暂停 / 取消 / 导出文件 |
| **文件路径显示** | 当前导入的 Excel 文件名和路径 |
| **进度条** | 翻译进度百分比 |
| **日志区** | 实时显示每条翻译状态 |

**使用步骤**：

28. 点击「**选择 Excel**」导入 `.xlsx` 台本文件
29. 弹出预览对话框显示前 5 行数据 + 统计信息（总行数、角色数量、情绪类型等）
30. Excel 需包含以下列（首行列名）：`台本ID, Character, Age, Gender, Body_Type, Emotion, Scene_Context, Text`
31. 点击「**开始翻译**」弹出设置对话框：

| 设置项 | 说明 |
|--------|------|
| **翻译模式** | 规则翻译（纯词库）/ 混合翻译（规则+AI）/ AI 翻译（全部AI） |
| **模型选择** | 选择使用的 AI 模型 |
| **并发控制** | 同时请求数（1-5）+ 间隔时间（0-10秒），避免 API 过载 |
| **自动添加新词** | 翻译完成后自动将 AI 创造的新词入库 |

32. 翻译过程中可**暂停**或**取消**
33. 进度条和日志实时更新
34. 完成后点击「**导出文件**」保存翻译结果 Excel
35. 弹出导出完成对话框，展示统计信息和操作按钮：
    - **查看新创词汇**：打开新创词汇报告，展示 AI 创造的所有新词（中文、自创语、IPA、TTS、构词逻辑）
    - **打开文件夹**：在文件管理器中打开导出目录
    - **打开文件**：直接打开导出的 Excel
    - **关闭**：关闭对话框
36. 未匹配词汇可导出为 CSV 报告，或通过未匹配词汇处理对话框手动/AI 填写

---

### 七、外观与主题设置

37. 点击菜单 **设置 → 外观与主题**

| 设置项 | 说明 |
|--------|------|
| **主题选择** | 下拉选择已注册主题，点击「应用」实时切换 |
| **字体族** | 选择系统字体，或选择「跟随主题默认」 |
| **字号缩放** | 滑条 80%~130%，步长 5%，实时预览字体大小 |
| **实时预览** | 底部预览区显示当前字体+字号效果 |
| **恢复默认** | 一键重置为跟随主题的默认字体 |

按钮栏：**应用**（保存并刷新） | **恢复默认** | **关闭**

应用自动适配 4K 高分辨率屏幕。

---

### 八、PaperHub AI 详细设置

点击菜单 **设置 → PaperHub 设置** 打开完整设置对话框：

| 设置项 | 说明 |
|--------|------|
| **启用 AI 辅助翻译** | 总开关，关闭后所有翻译使用纯规则 |
| **API Key** | 从 PaperHub 工作台获取，密码模式输入，可切换显示/隐藏 |
| **服务地址** | PaperHub API 地址（只读显示，默认值） |
| **模型选择** | 下拉选择 AI 模型；点击「刷新模型列表」从在线拉取最新可用模型 |
| **AI 使用策略** | 仅未匹配 / 全部 AI / 候选确认 |
| **思考模式** | 开启后 AI 进行链式推理，翻译质量更高但耗时增加（+30~90s） |
| **流式输出** | 推荐开启，AI 响应逐字显示，首 token 即时可见 |
| **Temperature** | 0~2，越高越随机，翻译建议 0.3~0.7 |
| **Max Tokens** | 单次响应最大 token 数，翻译建议 800~1500 |
| **请求超时** | 关闭思考建议 ≥60s；开启建议 ≥120s |
| **测试连接** | 验证 API Key 和模型是否可用 |

底部有「打开 PaperHub 工作台」按钮，可直接跳转获取 API Key。

---

## 快速参考卡片

| 我想做… | 去哪里 |
|---------|--------|
| 创建一门新语言 | 左侧语言区 → 「+ 新增」 |
| 导入词库/白皮书等资料 | 左侧资料区 → 「导入资料」 |
| 翻译一句话试试 | 右侧翻译页签 → 单句翻译 → 输入 → 点「翻译」 |
| 让 AI 翻译我没编好词的段落 | 先去设置启用 PaperHub → 单句翻译 |
| 翻译整个 Excel 台本 | 右侧翻译页签 → 批量翻译 → 选择 Excel → 开始翻译 |
| 和 AI 讨论语言设计 | 右侧语言大师问答页签 → 输入问题 → 发送 |
| 处理翻译中没匹配的词 | 翻译后点击统计行「将未匹配词添加到词库…」 |
| 把我的语言分享给别人 | 左侧语言列表 → 右键 → 导出语言包 |
| 切换界面主题 | 菜单 → 设置 → 外观与主题 |
| 配置 AI 翻译 | 菜单 → 设置 → PaperHub 设置 |

---

## 常见问题

| 问题 | 解答 |
|------|------|
| 翻译按钮点击无反应？ | 确保左侧选中了语言，且输入了中文文本 |
| 输出全是【词】？ | 词库尚未导入或词库中缺少对应词汇，请先导入词库资料 |
| AI 翻译失败？ | 检查 PaperHub 设置中 API Key 是否正确，网络是否畅通；可用「测试连接」验证 |
| 语言大师问答发送无反应？ | 确认 PaperHub 已启用且 API Key 已填写 |
| Excel 导入报错？ | 确保 Excel 首行包含必需列名（台本ID, Character, Age, Gender, Body_Type, Emotion, Scene_Context, Text），文件为 `.xlsx` 格式 |
| 如何共享语言？ | 右键语言→导出语言包，生成 ZIP；对方右键→导入语言包即可 |
| Token 消耗太快？ | 尝试关闭思考模式、降低 Max Tokens、使用「仅未匹配」策略减少 AI 调用量 |
| AI 回答内容不准确？ | 检查白皮书是否足够详细（音系规则、构词法、文化背景是关键）；尝试开启思考模式 |
| 批量翻译中途想停止？ | 点击「暂停」可暂定，点击「继续」恢复；点击「取消」终止本次翻译 |