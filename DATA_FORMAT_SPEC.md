# Nikki Conlang Forge 数据文件格式规范

> 本规范定义了翻译器四大资产文件的精确格式要求。
> 生成数据文件时**必须严格遵循**此规范，否则翻译器将无法解析，表现为"词库0条、映射0条、锚定0条"。

---

## 文件总览

每种自创语需要在 `data/{语言名}/` 目录下放置 4 个文件：

| 文件名 | 用途 | 格式 |
|--------|------|------|
| `whitepaper.md` | 语言逻辑白皮书（音系+语法） | Markdown |
| `Conlang_Master_Library.json` | 持久化词库 | JSON 扁平字典 |
| `Mapping_Rules.csv` | 音译映射（自创语→TTS友好拼写） | CSV 3列 |
| `Translation_History.json` | 翻译锚定历史 | JSON 键值字典 |

---

## 1. whitepaper.md — 语言逻辑白皮书

### 格式要求

- **纯 Markdown 文本**，UTF-8 编码
- 用 `# / ## / ###` 标题划分章节
- 翻译器会从以下标题名提取内容：
  - 包含 **"语法"** 的章节 → 提取语法规则
  - 包含 **"句法"** 的章节 → 提取句法规则
  - 包含 **"构词"** 的章节 → 提取构词规则
  - 全文中的 Markdown 表格 → 提取音位表（表头须含 `IPA` 或 `音位` 关键词）

### 音位表格式

用标准 Markdown 表格，表头必须包含 `IPA` 或 `音位`：

```markdown
| 字母 | IPA | 说明 |
|------|-----|------|
| a    | /ä/ | 开元音 |
| k    | /k/ | 清软腭塞音 |
```

### 语法规则格式

用列表格式（无序列表 `- / * / •` 或有序列表 `1. / (1)`），每条规则一行：

```markdown
## 语法规则

- 复数标记：词干辅音结尾加 -z，元音结尾加 -nz
- 过去时：词干 + -foznol
- 否定：na + 动词原形
```

### 示例骨架

```markdown
# 帕克索尔语逻辑白皮书

## 音系

| 字母 | IPA | 说明 |
|------|-----|------|
| a    | /ä/ | 开元音，类似"啊" |
| ö    | /ø/ | 前中圆唇元音 |
| k    | /k/ | 清软腭塞音 |
| z    | /ð/ | 浊齿间擦音（拼写z，发音如英语th） |

## 语法规则

- 复数标记：词干以辅音结尾直接加 -z，以元音结尾加 -nz
- 过去时后缀：-foznol
- 现在时后缀：-nol
- 未来时后缀：-böknol
- 否定前缀：na-

## 构词规则

- 形容词后缀：-i / -lik
- 名词化后缀：-ing / -skap / -sel
- 复合词：修饰词在前，核心词在后（如 drak-skal = 龙+声音 = 龙吟）

## 固定表达

- Sul hel → 灵魂安好 / 你好
- Zakil hel → 平安回家 / 再见
- Sul va → 魂啊！（仪式感叹）
```

---

## 2. Conlang_Master_Library.json — 持久化词库

### ⚠️ 最重要：必须是扁平字典

**键 = 中文词，值 = 自创语词**，整个文件就是一个 `{string: string}` 对象。

### 正确格式

```json
{
  "灵魂": "sul",
  "地": "gom",
  "龙": "drak",
  "声音": "skal",
  "你好": "Sul hel",
  "再见": "Zakil hel"
}
```

### ❌ 错误格式（翻译器无法解析）

以下格式**全部无法解析**，请勿使用：

```json
// ❌ 嵌套学术结构
{
  "language_info": { "name": "...", "version": "1.0" },
  "vocabulary": [
    { "root": "sul", "paxor_spelling": "sul", "chinese_meaning": "灵魂", "ipa_phonetic": "/sul/" }
  ]
}

// ❌ 对象数组
[
  { "chinese": "灵魂", "conlang": "sul" },
  { "chinese": "龙", "conlang": "drak" }
]

// ❌ 自创语做键（键不含中文→被跳过）
{
  "sul": "灵魂",
  "drak": "龙"
}
```

### 规则说明

1. **键必须是中文**（含 CJK 字符 U+4E00–U+9FFF），否则翻译器视为自创语而跳过
2. **值必须是自创语词**（不含中文的字符串）
3. **一个中文词可映射多个含义**（用 `/` 分隔）：`"地/土地": "gom"`
4. **不要嵌套**，不要数组，不要额外字段——纯 `{中文: 自创语}` 字典

### 翻译器识别的键名（仅用于数组/嵌套回退模式）

如果必须用对象数组格式，每个对象的键名须使用以下识别词：

| 中文侧识别词 | 自创语侧识别词 |
|-------------|--------------|
| `zh` `cn` `chinese` `中文` `source` `src` `han` `hanzi` `原文` `汉语` | `conlang` `target` `tl` `translation` `译` `译本` `译词` `词` `词条` `自创语` `目标语` |

### 可选：结构化条目（自 v2.x 起）

为支持 PaperHub AI 的检索式 prompt 注入与 token 优化，词库条目**可以**升级为结构化对象，附带词性、风格、核心词标记等元数据。**结构化与扁平条目可在同一文件中混用**，不破坏旧客户端读取（旧客户端会跳过 dict 值的条目）。

```json
{
  "灵魂": "sul",
  "光": {
    "conlang": "lumi",
    "pos": "名词",
    "style": "诗意",
    "core": false,
    "synonyms": ["光芒"],
    "freq": 12,
    "notes": "首批种子词"
  },
  "我": { "conlang": "ami", "core": true, "freq": 800 }
}
```

字段说明：

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `conlang` | string | **必填** | 自创语词；缺失则该条目被跳过（视同未定义） |
| `pos` | string | `""` | 词性（"名词"/"动词"/...）；展示在 prompt 词条行末尾 |
| `style` | string | `""` | 风格标签（"诗意"/"日常"/"古语"...） |
| `core` | bool | `false` | 标为 `true` 后该词在 PaperHub prompt 中**始终在���**（L1 核心词常驻） |
| `synonyms` | string[] | `[]` | 近义词列表；命中时通过 L3 扩展拉入 prompt |
| `freq` | int | `0` | 使用频率；`core=false` 时按 freq 排序补足核心词槽位 |
| `notes` | string | `""` | 备注，仅供编辑者参考，不参与 AI prompt |

兼容字段（亦可识别）：`pos→词性 / part_of_speech`、`style→风格 / register / tone`、`core→is_core / 核心 / 高频`、`synonyms→近义词 / syn`、`freq→frequency / 频率 / 使用次数`、`notes→备注 / note / 说明`。

### 兼容矩阵

| 文件形态 | 旧客户端 (v1.x) | 新客户端 (v2.x+) |
|---|---|---|
| 全扁平 `{zh: con_str}` | ✅ 正常 | ✅ 正常 |
| 全结构化 `{zh: {conlang, ...}}` | ⚠ 无法读取（值为 dict 被跳过） | ✅ ��常 + 元数据生效 |
| 混合（部分扁平 + 部分结构化） | ⚠ 仅扁平条目可读 | ✅ 全部可读 |

> **写盘策略**：本项目内置写词库的代码（手动添加 / AI 自动入库 / 批量翻译入库）**默认仍写扁平**，仅当某条目的元数据非空时该条目才写为结构化 dict。这样老语言包对老客户端始终兼容；只有用户手动启用结构化字段后，文件才会含 dict 值条目。


但**强烈建议直接用扁平字典**，避免回退模式的不确定性。

---

## 3. Mapping_Rules.csv — 音译映射

### 格式要求

- UTF-8 编码（允许 BOM）
- **3 列**，列名必须在翻译器识别范围内

### 必须的列名

| 列 | 识别关键词（列名包含任一即可） | 用途 |
|----|------|------|
| 第1列：自创语词汇 | `自创语词汇` `自创语` `词汇` `词形` | 自创语原文 |
| 第2列：IPA音标 | `IPA` `ipa` `音标` | IPA 国际音标（校验用） |
| 第3列：TTS友好拼写 | `TTS` `tts` `友好拼写` `拼写` | TTS引擎发音拼写 |

### 正确格式

```csv
自创语词汇,IPA音标,TTS友好拼写
sul,/sul/,sool
gom,/hom/,goh-mah
zakil,/ðakil/,thah-keel
drak,/drak/,drak
fozank,/foðank/,foh-thahnk
höka,/xöka/,kheu-kah
```

### ❌ 错误格式（翻译器无法解析）

```csv
// ❌ 列名不含识别关键词
帕克索尔语字母,IPA音标,发音描述,汉语近似,英语近似,TTS引擎友好拼写-Resemble,TTS引擎友好拼写-ElevenLabs

// ❌ 缺少 TTS 列
字母,IPA,说明

// ❌ TTS 填占位符（翻译器会将其当作有效值，但无实际用途）
sul,/sul/,sul
gom,/hom/,sul
```

### 规则说明

1. **第1列和第3列是必须的**，缺任一 → 解析直接报错
2. **第2列（IPA）可选但建议包含**，用于校验
3. **TTS友好拼写应填真实发音拼写**，不要填占位符（如重复填自创语原词）
4. 可以有更多列，但前三列须满足识别要求
5. 空行和空单元格的行会被跳过

---

## 4. Translation_History.json — 翻译锚定历史

### ⚠️ 推荐格式：`{"anchors": {...}}` 嵌套字典

```json
{
  "anchors": {
    "你好": "Sul hel",
    "再见": "Zakil hel",
    "魂啊": "Sul va",
    "我的魂": "Sul hö mi",
    "灵魂": "sul",
    "龙": "drak"
  }
}
```

### 翻译器解析优先级（从高到低）

| 优先级 | 格式 | 说明 |
|--------|------|------|
| 1 | `{"anchors": {"中文": "自创语"}}` | ✅ **推荐**，最稳定 |
| 2 | `[{"zh": "你好", "conlang": "Sul hel"}, ...]` | 数组 + 识别键名 |
| 3 | `{"history": [...同上...]}` 或 `{"records": [...]}`  | 嵌套数组 |
| 4 | `{"你好": "Sul hel", "再见": "Zakil hel"}` | 扁平字典（键含中文才提取） |

### ❌ 错误格式（翻译器无法解析）

```json
// ❌ 学术论文结构（键不含中文→0锚定）
{
  "document_info": { "title": "...", "version": "1.0" },
  "core_concepts_anchoring": [
    { "paxor_word": "sul", "chinese_translation": "灵魂", "anchoring_reason": "..." }
  ],
  "fixed_expressions_anchoring": [
    { "paxor_expression": "Sul hel", "chinese_translation": "你好" }
  ]
}

// ❌ 自创语做键
{
  "sul": "灵魂",
  "Sul hel": "你好"
}

// ❌ 空文件 {}
{}
```

### 规则说明

1. **锚定键必须是中文**（含 CJK 字符），值是自创语翻译结果
2. **固定表达/仪式用语必须包含**——这些是翻译器的"锚定"参照，确保核心词不会被错误翻译
3. 使用 `{"anchors": {...}}` 格式最安全，翻译器会直接读取 `anchors` 字典

### 如果必须用数组格式

每个对象的键名须使用以下识别词：

| 中文侧 | `zh` `cn` `chinese` `中文` `source` `原文` `汉语` `hanzi` |
|--------|------|
| 自创语侧 | `conlang` `target` `translation` `译` `译本` `自创语` `结果` |

---

## 速查对照表

| 检查项 | 正确 | 错误 |
|--------|------|------|
| 词库键 | 中文词（含汉字） | 自创语词/英文键 |
| 词库结构 | `{中文: 自创语}` 扁平字典 | 嵌套结构/对象数组 |
| CSV 列名 | 包含 `自创语词汇`/`词汇` + `TTS`/`拼写` | 用语言名做列名（如 `帕克索尔语字母`） |
| CSV TTS值 | 真实发音拼写（如 `goh-mah`） | 占位符（如 `sul`） |
| 锚定键 | 中文句/词 | 自创语句/词 |
| 锚定结构 | `{"anchors": {中文: 自创语}}` | 学术论文结构 |
| 白皮书音位表 | Markdown表格 + `IPA`/`音位`表头 | 纯文本描述 |
| 白皮书语法 | 列表格式（`-` / `1.`） | 散文段落 |

---

## 常见生成错误与修正

| AI常见生成倾向 | 问题 | 修正 |
|---------------|------|------|
| 给词库加 `language_info` / `vocabulary` 等包装层 | 翻译器走回退路径，键名不匹配→0词条 | 去掉包装层，直接输出 `{中文: 自创语}` |
| 用描述性键名 `chinese_meaning` / `paxor_spelling` | 不在翻译器识别词列表中→跳过 | 改为 `中文` / `自创语`，或直接用扁平字典 |
| CSV 用语言名做列名（`XX语字母`） | 不含 `自创语词汇` 等关键词→报错 | 列名改为 `自创语词汇` |
| TTS列全部填占位符 | 无实际发音参考价值 | 填写真实TTS友好拼写 |
| 锚定用学术论文结构 | 键不含中文→0锚定 | 用 `{"anchors": {中文: 自创语}}` |
| 锚定用 `paxor_word` / `chinese_translation` 键名 | 不在识别词列表→跳过 | 改为 `zh` / `conlang`，或用 anchors 字典 |

---

## 完整示例：帕克索尔语

以下为翻译器能正确解析的帕克索尔语四文件完整示例：

### whitepaper.md

```markdown
# 帕克索尔语逻辑白皮书（Paxor Language）

## 音系

### 元音

| 字母 | IPA | 说明 |
|------|-----|------|
| a    | /ä/ | 开元音，类似"啊"但唇位更中性 |
| e    | /e/ | 前中不圆唇元音 |
| i    | /i/ | 前高不圆唇元音 |
| o    | /o/ | 后中圆唇元音 |
| ö    | /ø/ | 前中圆唇元音 |
| u    | /u/ | 后高圆唇元音 |
| ü    | /y/ | 前高圆唇元音 |

### 辅音

| 字母 | IPA | 说明 |
|------|-----|------|
| p    | /p/ | 清双唇塞音 |
| t    | /t/ | 清齿龈塞音 |
| k    | /k/ | 清软腭塞音 |
| m    | /m/ | 双唇鼻音 |
| n    | /n/ | 齿龈鼻音 |
| f    | /f/ | 清唇齿擦音 |
| v    | /v/ | 浊唇齿擦音 |
| s    | /s/ | 清齿龈擦音 |
| z    | /ð/ | 浊齿间擦音（拼写z，发音如英语th） |
| g    | /h/ | 清声门擦音（词首弱读） |
| j    | /ʒ/ | 浊卷舌擦音 |
| r    | /r/ | 齿龈颤音（弹舌音） |
| l    | /l/ | 齿龈边近音 |
| h    | /x/ | 清软腭擦音 |
| w    | /w/ | 双唇软腭近音 |

### 复合音

| 组合 | IPA | 说明 |
|------|-----|------|
| au   | /øy/ | 由/ø/滑向/y/ |
| ei   | /ei/ | 由/e/滑向/i/ |
| öu   | /øu/ | 由/ø/滑向/u/（仪式语言常用） |
| vl-  | /vl/ | 如 vlön（从） |
| fz-  | /fð/ | 如 foznol（过去时） |
| kh-  | /x/  | 如 Khün（怎么） |
| kv-  | /kv/ | 如 Kval（为什么） |
| sk-  | /sk/ | 如 skal（声音） |
| st-  | /st/ | 如 sten（石） |

## 语法规则

- 复数标记：词干以辅音结尾直接加 -z，以元音结尾加 -nz；发音为 /ð/
- 过去时后缀：-foznol（发音 /foðnol/）
- 现在时后缀：-nol
- 未来时后缀：-böknol（发音 /bøknol/）
- 否定前缀：na-（na + 动词原形）
- 疑问词首字母大写：Kul（谁）、Kas（什么）、Kör（哪里）、Kval（为什么）、Khün（怎么）、Kel（哪个）

## 构词规则

- 形容词后缀：-i（一般性质）、-lik（类比性质，如 draklik = 龙一般的）
- 名词化后缀：-ing（动作名词）、-skap（抽象名词）、-sel（契约/制度名词）
- 动词化后缀：-a（从名词/形容词派生动词，如 skala = 发声）
- 职业/角色后缀：-er（如 zaker = 引导者、höker = 观测者）
- 复合词：修饰词在前 + 核心词在后（如 drak-skal = 龙+声音 = 龙吟）

## 固定表达

- Sul hel → 灵魂安好 / 你好（日常问候）
- Zakil hel → 平安回家 / 再见（离别祝福）
- Sul va → 魂啊！（仪式感叹）
- Sul hö mi → 我的魂!!（最深哀悼）
- Sul zaka → 引渡灵魂（仪式宣告）
- Fozank mova → 祖先的爱 / 感谢（感恩表达）
- na mova → 不爱（否定情绪）
```

### Conlang_Master_Library.json

```json
{
  "灵魂": "sul",
  "地": "gom",
  "埋骨地": "gom?",
  "引导": "zaka",
  "回家": "zakil",
  "龙": "drak",
  "声音": "skal",
  "看见": "höka",
  "感知": "höka",
  "生命": "lif",
  "死亡": "död",
  "爱": "mova",
  "家庭": "fam",
  "氏族": "fam",
  "朋友": "ven",
  "敌人": "fien",
  "前": "foz",
  "过去": "foz",
  "后": "böka",
  "未来": "bök",
  "现在": "nol",
  "时间": "tid",
  "神圣": "hel",
  "仪式": "rit",
  "承诺": "löva",
  "契约": "löva",
  "歌": "sang",
  "咏唱": "sang",
  "水": "vat",
  "火": "eld",
  "石": "sten",
  "树": "trä",
  "木": "trä",
  "我": "mi",
  "你": "ti",
  "他": "hon",
  "她": "hon",
  "它": "hon",
  "祂": "hon",
  "他们": "honz",
  "她们": "honz",
  "它们": "honz",
  "祂们": "honz",
  "我们": "miz",
  "你们": "tiz",
  "这": "se",
  "那": "so",
  "谁": "Kul",
  "什么": "Kas",
  "哪里": "Kör",
  "为什么": "Kval",
  "怎么": "Khün",
  "哪个": "Kel",
  "自己": "ön",
  "某": "nun",
  "在": "ro",
  "到": "tul",
  "从": "vlön",
  "和": "dö",
  "上": "vör",
  "下": "nön",
  "左": "vans",
  "右": "rüs",
  "必须": "zekva",
  "如果": "öm",
  "也许": "setva",
  "不": "na",
  "非": "na",
  "魂啊": "Sul va",
  "我的魂": "Sul hö mi",
  "引渡灵魂": "Sul zaka",
  "你好": "Sul hel",
  "灵魂安好": "Sul hel",
  "平安回家": "Zakil hel",
  "再见": "Zakil hel",
  "祖先的爱": "Fozank mova",
  "感谢": "Fozank mova",
  "不爱": "na mova",
  "不活": "na lif",
  "活着": "lifa",
  "死去": "döda",
  "生命的": "lifi",
  "死亡的": "dödi",
  "古老的": "fjali",
  "新的": "nuli",
  "深的": "djuni",
  "灵魂的": "suli",
  "龙一般的": "draklik",
  "龙吟": "drakskal",
  "发声": "skala",
  "观测者": "höker",
  "引导者": "zaker",
  "造物": "skapsel",
  "爱(名词)": "moving",
  "家族": "famil",
  "家人": "famer",
  "友谊": "venskap",
  "敌意": "fienskap",
  "祖先": "fozank",
  "圣地": "helgom",
  "契约(名词)": "lövsel",
  "灵魂之地": "sulgom",
  "冥界": "sulgom",
  "灵魂之路": "sulväg",
  "引渡": "sulzaka"
}
```

### Mapping_Rules.csv

```csv
自创语词汇,IPA音标,TTS友好拼写
sul,/sul/,sool
gom,/hom/,goh-mah
zakil,/ðakil/,thah-keel
drak,/drak/,drak
fozank,/foðank/,foh-thahnk
höka,/xöka/,kheu-kah
zekva,/ðekva/,thek-vah
miz,/mið/,meeth
khün,/xyn/,khuen
vlön,/vlön/,vleun
böknol,/bøknol/,beuk-nohl
sten,/sten/,sten
trä,/træ/,trae
```

### Translation_History.json

```json
{
  "anchors": {
    "你好": "Sul hel",
    "再见": "Zakil hel",
    "魂啊": "Sul va",
    "我的魂": "Sul hö mi",
    "引渡灵魂": "Sul zaka",
    "灵魂安好": "Sul hel",
    "平安回家": "Zakil hel",
    "祖先的爱": "Fozank mova",
    "感谢": "Fozank mova",
    "不爱": "na mova",
    "不活": "na lif",
    "灵魂": "sul",
    "地": "gom",
    "龙": "drak",
    "声音": "skal",
    "看见": "höka",
    "生命": "lif",
    "死亡": "död",
    "爱": "mova",
    "神圣": "hel",
    "仪式": "rit",
    "必须": "zekva",
    "怎么": "Khün",
    "从": "vlön",
    "祖先": "fozank"
  }
}
```

---

## 一句话总结

> **中文做键，自创语做值，扁平字典不嵌套，CSV列名含关键词，TTS填真实拼写。**